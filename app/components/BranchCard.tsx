"use client";
import {motion} from "framer-motion";import {ArrowUpRight,MapPin,MessageCircle} from "lucide-react";import type {Branch} from "@/data/branches";import {createWhatsAppUrl} from "@/lib/whatsapp";import {trackEvent} from "@/lib/analytics";
export function BranchCard({branch,index}:{branch:Branch;index:number}){let url="";try{url=createWhatsAppUrl(branch.whatsapp,branch.message)}catch{if(process.env.NODE_ENV==="development")console.error(`Invalid WhatsApp number for ${branch.name}`)}if(!url)return null;const click=()=>trackEvent("whatsapp_click",{branch:branch.name,phone:branch.whatsapp,source:"landing_page"});return <motion.article className="branchCard" initial={{opacity:0,y:24}} whileInView={{opacity:1,y:0}} viewport={{once:true,amount:.2}} transition={{delay:(index%4)*.06,duration:.55,ease:[.22,1,.36,1]}}><div className="locationIcon"><MapPin/></div><span className="availability"><i/>WhatsApp ordering</span><h3>{branch.name}</h3><a className="phone" href={`tel:+${branch.whatsapp}`}>{branch.phone}</a><a href={url} onClick={click} target="_blank" rel="noopener noreferrer" className="whatsapp" aria-label={`Chat with Cake City ${branch.name} on WhatsApp`}><MessageCircle/>Chat with this branch <ArrowUpRight/></a></motion.article>}
