# Cake City WhatsApp Campaign Landing Page

A focused paid-ad landing page that routes customers to the correct Cake City branch on WhatsApp.

```powershell
npm.cmd install
npm.cmd run dev
```

Open http://127.0.0.1:3200. Analytics events are pushed to `window.dataLayer` when available and remain harmless when analytics IDs are absent.
